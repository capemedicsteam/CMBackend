{
  {%for employee in employees%}
  "{{employee.id}}" : {
                        "name": "{{employee.name}}",
                        "number": "{{employee.number}}",
                        "email": "{{employee.email}}",
                        "qualification": "{{employee.qualification}}",
                        "last_sign_in": "{{employee.lastSignIn}}",
                        "licence_expiry_date": {"colour":"{{employee.licenceExpiryDateColour}}", "text":"{{employee.licenceExpiryDateText}}"}
  }{%if not loop.last%},{%endif%}
  {%endfor%}
}
